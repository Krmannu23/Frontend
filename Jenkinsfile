
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Automatic Build'
            }
        }
         stage('Test') {
            steps {
                echo 'Automatic Test'
            }
        }
    }
}
